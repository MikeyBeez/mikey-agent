{
  "name": "mcp-brain-assistant",
  "version": "1.0.0",
  "description": "Natural language assistant for the Brain system",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "npm run build && node dist/index.js --test",
    "clean": "rm -rf dist",
    "streamlit": "streamlit run ui/app.py",
    "jest": "jest"
  },
  "keywords": [
    "mcp",
    "brain",
    "assistant",
    "ai"
  ],
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "@modelcontextprotocol/sdk": "^1.17.3",
    "dotenv": "^16.0.3",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
